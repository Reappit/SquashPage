name: squash_page
description: "Project which helps to track squash tournaments"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.3.4
  intl_utils: ^2.8.7
  flutter_hooks: ^0.20.5
  hooks_riverpod: ^2.4.10
  riverpod_annotation: ^2.3.4

  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

  auto_route: ^7.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

  auto_route_generator: ^7.3.2
  build_runner: ^2.4.8
  freezed: ^2.4.7
  json_serializable: ^6.7.1
#  dart_code_metrics: ^5.7.6
  riverpod_generator: ^2.3.11
#  riverpod_lint: ^2.3.9

flutter_intl:
  enabled: true
  main_locale: ru

flutter:

  uses-material-design: true
